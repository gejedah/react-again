import { Form } from 'antd';

export default function LoginPage() {
    return <div>
        <Form>
            <Form.Item>
                <p>로그인 Login</p>
            </Form.Item>
        </Form>
    </div>;
}